# PostgreSQL
DB_NAME=timemanager
DB_USER=timemanager
DB_PASSWORD=timemanager

# VAPID (Web Push) — 아래 명령으로 생성:
#   npx web-push generate-vapid-keys
PUSH_VAPID_PUBLIC_KEY=
PUSH_VAPID_PRIVATE_KEY=
PUSH_VAPID_SUBJECT=mailto:admin@example.com
