<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: OAuth 콜백만 앱으로 가로챈다. autoVerify 는 도메인의
                 /.well-known/assetlinks.json 지문과 앱 서명이 일치할 때만 통과한다. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="timemanager.mungji.com" android:pathPrefix="/oauth/callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- API 33+ 는 알림 표시에 런타임 권한이 필요하다. 32 이하는 자동 부여. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      ⚠️ USE_EXACT_ALARM / SCHEDULE_EXACT_ALARM 은 선언하지 않는다.
      Play 가 알람·시계·캘린더 앱에만 허용하는 제한 권한이라 선언하는 순간 심사 폼이 붙고,
      거절되면 릴리스 자체가 막힌다. 이 앱의 알림("3시간 되었습니다", "목표 도달")은
      몇 분 늦어도 무해하므로 inexact 스케줄링으로 간다
      (= LocalNotifications 호출에서 allowWhileIdle 을 켜지 않는다).
    -->
</manifest>
